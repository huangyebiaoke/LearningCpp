#include <iostream>
#include <string.h>
using namespace std;
/**
 * strcat(char[],const char[]):字符串连接数组
 * strcpy(char[],const char[]):将第二个字符串复制到第一个字符数组中
 * strcmp(const char[],const char[]):字符串数组 长短比较
 */
int main(int argc, char const *argv[]){
    // char str[20];
    // cin.getline(str,20,'.');
    // cout<<str<<" I"<<strlen(str)<<endl;
    cout<<strcmp("IAS","IAM")<<endl;
    return 0;
}